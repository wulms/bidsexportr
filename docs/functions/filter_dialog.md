# `filter_dialog`

Starts the "filter dialog" for sequence, session and regular expressions in the BIDS dataset


## Description

Starts the "filter dialog" for sequence, session and regular expressions in the BIDS dataset


## Usage

```r
filter_dialog()
```


